name: bmi_calculator
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: '>=2.2.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  font_awesome_flutter: ^8.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.7.2

flutter_icons:
  android: 'launcher_icon'
  ios: true
  image_path: 'assets/icon/health.png'
flutter:
  uses-material-design: true
